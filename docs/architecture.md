# Architecture Overview

Phase 0 focuses on a modular, production-oriented backend architecture using FastAPI, async SQLAlchemy, and WebSockets for realtime updates. AI models and analytics will live in the `ml_models/` workspace and integrate via REST or RPC.
